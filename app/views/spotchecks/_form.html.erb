<%= form_for(@spotcheck) do |f| %>
  <% if @spotcheck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spotcheck.errors.length, "error") %> prohibited this spotcheck from being saved:</h2>

      <ul>
      <% @spotcheck.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job_id %><br />
    <%= f.number_field :job_id %>
  </div>
  <div class="field">
    <%= f.label :service_partner_id %><br />
    <%= f.number_field :service_partner_id %>
  </div>
  <div class="field">
    <%= f.label :rating %><br />
    <%= f.number_field :rating %>
  </div>
  <div class="field">
    <%= f.label :rating_text %><br />
    <%= f.text_area :rating_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
