<%= form_for(@job_status) do |f| %>
  <% if @job_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job_status.errors.count, "error") %> prohibited this job_status from being saved:</h2>

      <ul>
      <% @job_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
